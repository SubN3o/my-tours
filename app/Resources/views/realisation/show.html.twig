{% extends '::layout_admin.html.twig' %}

{% block content %}

    <div class="content">
        <div class="container-fluid">
            <div class="row">

                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header light-green">
                            <h4 class="title">Realisation : {{ realisation.nom }}</h4>
                        </div>
                        <div class="card-content table-responsive">
                            <table class="table">
                                <tbody>
                                <tr>
                                    <th>Tri</th>
                                    <td>{{ realisation.tri }}</td>
                                </tr>
                                <tr>
                                    <th>Nom</th>
                                    <td>{{ realisation.nom }}</td>
                                </tr>
                                <tr>
                                    <th>Ville</th>
                                    <td>{{ realisation.ville }}</td>
                                </tr>
                                </tbody>
                            </table>
                            <hr>
                            <div class="row">
                                <div class="col s4">
                                    <img class="responsive-img"
                                         src="{{ asset('uploads/'~realisation.media.mediaName) }}"
                                         alt="{{ realisation.nom }}"/>
                                    <p>{{ realisation.media.typeMedia.nom }}</p>
                                </div>
                            </div>
                            <ul>
                                <li>
                                    <a href="{{ path('admin_realisation_index') }}"><i class="material-icons">keyboard_return</i>Retour à la liste des realisations</a>
                                </li>
                                <li>
                                    <a href="{{ path('admin_realisation_edit', { 'id': realisation.id }) }}"><i
                                                class="material-icons">build</i>Éditer ce realisation</a>
                                </li>

                                <li>
                                    {{ form_start(delete_form) }}
                                    <input type="submit" value="Supprimer" class="waves-effect waves-light btn-large red pull-right">
                                    {{ form_end(delete_form) }}
                                </li>
                            </ul>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
