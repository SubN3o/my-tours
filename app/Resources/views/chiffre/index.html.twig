{% extends '::layout_admin.html.twig' %}

{% block content %}
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header light-green">
                            <h4 class="title">Liste des chiffres pour la page my orléans</h4>
                        </div>
                        <div class="card-content table-responsive">
                            <table class="table">
                                <thead>
                                <tr>
                                    <th>Tri</th>
                                    <th>Nom</th>
                                    <th>Valeur</th>
                                </tr>
                                </thead>
                                <tbody>

                                {% for chiffre in chiffres %}

                                    <tr>
                                        <td>{{ chiffre.tri }}</td>
                                        <td>{{ chiffre.nom }}</td>
                                        <td>{{ chiffre.valeur }}</td>
                                        <td>
                                            <a href="{{ path('admin_chiffre_show', { 'id': chiffre.id }) }}"><i
                                                        class="material-icons">remove_red_eye</i></a>
                                        </td>
                                        <td>
                                            <a href="{{ path('admin_chiffre_edit', { 'id': chiffre.id }) }}"><i
                                                        class="material-icons">build</i></a>
                                        </td>
                                    </tr>

                                {% endfor %}

                                </tbody>
                            </table>
                            <hr>
                            <div class="navigation center">
                                {{ knp_pagination_render(chiffres, 'MyOrleansBundle::pagination.html.twig') }}
                            </div>
                            <ul>
                                <li>

                                    <a href="{{ path('admin_chiffre_new') }}"
                                       class="btn-floating btn-large waves-effect waves-light light-green"><i
                                                class="material-icons">add</i>Ajouter une
                                        chiffre</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

