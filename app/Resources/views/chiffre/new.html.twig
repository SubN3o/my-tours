{% extends '::layout_admin.html.twig' %}

{% block content %}

    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header light-green">
                            <h4 class="title"> Ajouter une nouveau chiffre</h4>
                        </div>
                        <div class="card-content table-responsive">

                            {{ form_start(form) }}
                            <div class="row">
                                <div class="col s1">
                                    {{ form_label(form.tri, 'Tri :') }}
                                    {{ form_widget(form.tri) }}
                                    {{ form_errors(form.tri) }}
                                </div>
                            </div>
                            <div class="row">
                                <div class="col s6">
                                    {{ form_label(form.nom, 'Nom :') }}
                                    {{ form_widget(form.nom) }}
                                    {{ form_errors(form.nom) }}
                                </div>
                                <div class="col s1">
                                    {{ form_label(form.valeur, 'Valeur :') }}
                                    {{ form_widget(form.valeur) }}
                                    {{ form_errors(form.valeur) }}
                                </div>
                            </div>
                            <input class="waves-effect waves-light btn-large light-green" type="submit" value="Créer"/>
                            {{ form_end(form) }}

                            <ul>
                                <li>
                                    <a href="{{ path('admin_chiffre_index') }}"><i class="material-icons">keyboard_return</i>Retour à la liste des chiffres</a>
                                </li>
                            </ul>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}