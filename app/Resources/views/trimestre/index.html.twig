{% extends '::layout_admin.html.twig' %}

{% block content %}
    <div class="content">
        <div class="container-fluid">
            <div class="row">

                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header light-green">
                            <h4 class="title">Liste des trimestres dans lesquelles se trouvent les résidences</h4>
                        </div>
                        <div class="card-content table-responsive">
                            <table class="table">
                                <thead class="text">
                                <th>Id</th>
                                <th>Nom</th>
                                </thead>
                                <tbody>

                                {% for trimestre in trimestres %}

                                    <tr>
                                        <td><a href="{{ path('admin_trimestre_show', { 'id': trimestre.id }) }}">{{ trimestre.id }}</a></td>
                                        <td>{{ trimestre.nom }}</td>
                                        <td>
                                            <a href="{{ path('admin_trimestre_show', { 'id': trimestre.id }) }}"><i
                                                        class="material-icons">remove_red_eye</i></a>
                                        </td>
                                        <td>
                                            <a href="{{ path('admin_trimestre_edit', { 'id': trimestre.id }) }}"><i
                                                        class="material-icons">build</i></a>
                                        </td>
                                    </tr>

                                {% endfor %}

                                </tbody>
                            </table>
                            <hr>
                            {#<div class="navigation center">#}
                                {#{{ knp_pagination_render(trimestres, 'MyOrleansBundle::pagination.html.twig') }}#}
                            {#</div>#}
                            <ul>
                                <li>

                                    <a href="{{ path('admin_trimestre_new') }}"
                                       class="btn-floating btn-large waves-effect waves-light light-green"><i
                                                class="material-icons">add</i>Ajouter une
                                        trimestre</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
